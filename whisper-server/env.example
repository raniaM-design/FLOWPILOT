# Configuration du serveur Whisper PILOTYS

# Modèle Whisper à utiliser
# Options: tiny, base, small, medium, large
# Recommandation: base (bon équilibre vitesse/qualité)
WHISPER_MODEL=base

# Langue par défaut pour la transcription
# Options: fr (français), en (anglais), auto (auto-détection)
WHISPER_LANGUAGE=fr

# Clé API pour sécuriser l'accès au serveur
# Générez une clé aléatoire sécurisée (ex: openssl rand -hex 32)
WHISPER_API_KEY=votre_cle_secrete_ici_changez_moi

# Activer l'authentification (true/false)
REQUIRE_AUTH=true

# Origines autorisées pour CORS (séparées par des virgules)
# Remplacez par votre domaine PILOTYS
ALLOWED_ORIGINS=https://votre-domaine-pilotys.vercel.app,http://localhost:3000

# Taille maximale des fichiers audio (en MB)
MAX_FILE_SIZE_MB=25

# Port sur lequel le serveur écoute
PORT=8000

# Host sur lequel le serveur écoute (0.0.0.0 pour toutes les interfaces)
HOST=0.0.0.0

# Mode debug (true pour développement, false pour production)
DEBUG=false

